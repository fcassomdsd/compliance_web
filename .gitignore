node_modules/
dist/
*~
coverage/
eslint-report.json
.npm/
.env
.env.*
!.env.docker.example

# Self-signed certificates for local TLS verification (never commit a key)
docker/nginx/certs/

# throwaway build context created by the validate:edge CI job
certctx/
